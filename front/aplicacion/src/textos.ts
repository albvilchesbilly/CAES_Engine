/**
 * Todo el texto de interfaz de esta pagina, en un solo sitio (mismo criterio que `workspace/textos.ts`).
 *
 * Son cuatro frases, y tres de ellas existen para lo mismo: que nadie confunda esto con un sistema
 * autenticado. La cuarta es el enlace de vuelta a la cola.
 */

export const TITULO_APLICACION = "CAE Engine — workspace";

/** Lo que esta pagina dice de si misma, siempre, esté como esté configurada. */
export const AVISO_AUTENTICACION =
  "AUTENTICACIÓN DE DESARROLLO: esta página declara quién eres en una cabecera y el servidor no " +
  "comprueba ninguna credencial. Solo sirve contra datos sintéticos, solo en local.";

export const ETIQUETA_DECLARADO = "Esta página se declara";

export const VOLVER_A_LA_COLA = "Volver a la cola";

export const TITULO_SIN_PRINCIPAL = "No hay ningún principal declarado";

export const SIN_PRINCIPAL_DESCRIPCION =
  "Sin principal no se pide nada al servidor: la página no se autentica sola. Declara quién eres al " +
  "arrancar el empaquetador y vuelve a cargar.";
