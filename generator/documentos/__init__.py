"""Un render por tipo de documento (docs/01 §3.4). Cada módulo expone `seccion(caso, ...) -> Seccion`;
`base.construir_pdf` convierte una o varias secciones en un PDF con la marca sintética en cada página."""
