# Propuesta de cambio: hallazgos normativos del cierre de la Fase 0 (`ADR-003`)

**Estado: PROPUESTA, NO APROBADA.** Regla de oro 9: ningún cambio en la spec activa sin revisión de Billy. Este fichero **describe** el diff; no modifica `spec/IND240_v1.1.yaml`.

Origen: `docs/decisiones/ADR-003-hallazgos-normativos-fase-0.md` (revisión normativa exigida por `docs/06` §1, HEAD `84c90a4`).

**Relación con `spec/propuestas/IND240_v1.2.diff.md`**: independiente y acumulable. Aquel diff reorganiza la ficha alrededor de `cabecera_v1.yaml` y propone INT-08 e INT-09; este solo **declara criterios que hoy vive el código sin que la spec los diga**. Si ambos se aprueban, el orden natural es aplicar primero este (no cambia la estructura) y después el v1.2. Ningún punto de este diff toca las filas 1-13 de aquel.

**Ninguno de estos cambios altera los 7 veredictos ni el AETOTAL del banco de pruebas**, salvo donde la tabla lo diga expresamente.

---

## A. Interpretaciones nuevas (bloque `interpretaciones:`)

Añadir INT-11..INT-15 con el texto YAML íntegro de `ADR-003` §«Interpretaciones nuevas propuestas», y sustituir la entrada de INT-10 (hoy solo en `ADR-002` §3 y en `engine/reglas.py::ALIAS_CONTEXTO`) por la redacción de `ADR-003` §«INT-10 en forma».

| INT | Tema | Impacto | Efecto en los 7 casos |
|---|---|---|---|
| INT-10 | Qué fecha es «la fecha de solicitud» en prevalidación | medio | Ninguno (ya se aplica; solo se documenta) |
| INT-11 | Qué es «el periodo» de un registro de funcionamiento | medio | Ninguno (ya se aplica) |
| INT-12 | Carácter anual y representatividad de `h_antes` | alto | Ninguno (ya se aplica) |
| INT-13 | Con qué columna se entra al cuadro 6 | medio | Ninguno (ya se aplica) |
| INT-14 | Sustitución en el cálculo provisional del menor de `h` | medio | Ninguno (ya se aplica; caso B sigue en 305.829) |
| INT-15 | Cómo se reconoce documentalmente un «equipo nuevo» | alto | Ninguno (ya se aplica) |

Declararlas **no cambia nada del cálculo**: hace visible en el informe lo que hoy el motor decide sin decirlo.

---

## B. Cambios en variables y cálculo

| # | Elemento | v1.1 (actual) | Propuesto | Motivo (hallazgo) | Riesgo |
|---|---|---|---|---|---|
| B1 | `variables.h_antes` | Sin `interpretacion` | `interpretacion: INT-12` | H-03: se consume como horas anuales sin criterio declarado | Ninguno; añade INT-12 a `interpretaciones_aplicadas` |
| B2 | `variables.h_despues.derivacion` | `interpretacion: INT-04` | `interpretacion: [INT-04, INT-11]` (o `interpretaciones:` si se admite lista) | H-02: el periodo es una razón de filas, no de tiempo | Requiere que el registro admita más de un INT por derivación (cambio de marco pequeño) |
| B3 | `variables.N2.derivacion` | `interpretacion: INT-03`, `periodo_minimo_dias: 30` | `interpretacion: [INT-03, INT-11]`; `periodo_minimo_dias` **lo lee `R-EVD-01`** o se retira | H-01, H-10: el campo hoy está muerto y el 30 está duplicado | Si `R-EVD-01` lo lee, cambiar el 30 en un sitio cambia la regla: eso es lo que se quiere |
| B4 | `variables.perdidas_ref_kw.derivacion` | `clave: PM`, `si_no_existe_fila: INT-02` | + `interpretacion: INT-13`; + `si_fuera_de_rango: no_calcula` | H-04, H-11: la columna de entrada y el «fuera de rango no extrapola» solo viven en el código | Ninguno |
| B5 | `variables.h.derivacion` | `metodo: "min(h_antes, h_despues)"` | + `interpretacion_provisional: INT-14` | H-05: el criterio del provisional no está en ninguna parte de la spec | Ninguno |
| B6 | `calculo` | `aritmetica: decimal_exacta` | + `precision_decimal: 34` | H-15: constante numérica del motor que ya demostró mover el resultado (QA H9) | Ninguno si el motor la lee con 34 por defecto |

---

## C. Reglas

| # | Regla | v1.1 (actual) | Propuesto | Motivo (hallazgo) | Riesgo |
|---|---|---|---|---|---|
| C1 | **`R-CON-08`** (nueva) | No existe | `descripcion: "P_prom declarado coincide con P_prom derivado del registro (±0,5 kW)"` · `logica: "abs(P_prom.declarado - P_prom.derivado) <= 0.5"` · `severidad: SUBSANABLE` · `fase: consistencia` · `referencia: SRC-FICHA §5.5.b` | H-09: la spec declara `tolerancia_cruce_kw: 0.5` y **ninguna regla la consume**; el cruce que la ficha exige al certificado no se hace | **Puede cambiar el ground truth** si el `P_prom` del certificado sintético no cuadra con el derivado dentro de 0,5 kW. Comprobar en los 7 casos antes de aprobar. Alternativa: retirar `tolerancia_cruce_kw` de la spec |
| C2 | `R-AMB-01` | `logica: "tipo_equipo_accionado in ambito.tipos_equipo_incluidos"` · `BLOQUEANTE_AMBITO` | Desdoblar: `R-AMB-01` → `not (tipo_equipo_accionado in ambito.tipos_equipo_excluidos)` · `BLOQUEANTE_AMBITO` · `referencia: EXC-03`; y `R-AMB-01B` → `tipo_equipo_accionado in ambito.tipos_equipo_incluidos` · **`SUBSANABLE`** | H-16: hoy un tipo **no reconocido** (error de extracción) recibe el mismo `NO_ELEGIBLE` que uno excluido por la ficha, y `tipos_equipo_excluidos` es lista muerta | Cambia el veredicto de una actuación con tipo ilegible de `NO_ELEGIBLE` a `SUBSANABLE`. El caso D declara un tipo **excluido explícito**, así que sigue `NO_ELEGIBLE`: verificar en `evaluar_casos.py` |
| C3 | `R-EVD-01` | `logica: "registro.dias >= 30"` | `logica: "registro.dias >= N2.derivacion.periodo_minimo_dias"` (o mantener el literal y retirar el campo) · + `interpretacion: INT-11` | H-01, H-10 | Requiere que el contexto resuelva rutas de la spec; si no, mantener el literal y retirar `periodo_minimo_dias` |
| C4 | `R-AMB-02` | `logica: "not exists(factura.linea where categoria in [motor, bomba, ventilador, compresor, equipo_completo])"` | Igual + `interpretacion: INT-15`; y el **léxico** que asigna `categoria` pasa de `engine/extraccion.py` a un bloque `lexico:` de la ficha | H-06: una heurística léxica en `engine/` decide `NO_ELEGIBLE`, y es vocabulario de ficha en el núcleo (regla de oro 4) | El movimiento del léxico es cambio de marco: propio de v1.2, con su propio test |
| C5 | `R-DOC-02` | Sin `interpretacion` | + `interpretacion: INT-15` (o un INT propio si Billy prefiere separarlos) | H-17: el léxico que decide «foto antes» / «foto después» también vive en `engine/` | Ninguno (`SUBSANABLE`) |
| C6 | `R-TMP-03` | `logica: "solicitud.fecha <= fecha_fin_actuacion + 3 años"` · `AVISO` · sin `interpretacion` | + `interpretacion: INT-08`, y añadir a INT-08: cómputo por meses de calendario con recorte de día, límite inclusivo | H-07: el cómputo efectivo de v1.1 no está declarado en ninguna parte y el v1.2 propone otro distinto | Ninguno hoy (`AVISO`) |
| C7 | `R-TMP-02` | `logica: "convenio.fecha_firma <= solicitud.fecha"` · `SUBSANABLE` | + `interpretacion: INT-10` y nota de que, bajo el criterio actual, **no puede fallar** | H-08 | Ninguno; evita presentar como comprobado lo que no se ha puesto a prueba |
| C8 | `DOC-05B` | `obligatorio: condicional` · `condicion: "instalacion_personal_propio == true"` | Declarar `instalacion_personal_propio` como variable (`nivel: expediente`, `evidencia: declarado`, `fuentes: [ficha_cumplimentada, certificado_instalador]`) | H-19 y `ADR-002` §6.4: ningún extractor produce el dato, así que el documento se da por no obligatorio con un aviso mudo | Si el dato falta, `R-DOC-01` pasaría a `NO_EVALUABLE`; requiere decidir antes la alternativa de `ADR-002` §6.4 |

---

## D. Documentación (no es spec, va con el mismo lote)

| # | Fichero | Cambio | Motivo |
|---|---|---|---|
| D1 | `docs/04` §13.1 y §13.2 | Añadir INT-10..INT-15 a la tabla de propuestas; mover a §13.1 las que se aprueben | A, C |
| D2 | `docs/04` §13, primer párrafo | «el informe lista las interpretaciones que **han influido** en el resultado» no describe lo que hace el código (lista las **citadas** por reglas evaluadas). Corregir el texto o el código | H-18, `ADR-002` §6.5 |
| D3 | `docs/HUECOS.md`, fila `API-12` | Ampliar «nuestros INT-01..05» a «INT-01..05 y INT-11..INT-15» | `ADR-003` §Consecuencias. **No se abre ningún `API-xx` nuevo** |
| D4 | `data/README.md`, «Sobre la columna `cos_phi`» | La afirmación «el cuadro 6 no contiene columna de factor de potencia» es de memoria del agente y tiene un indicio en contra (factor de desplazamiento de la carga de ensayo). Marcarla como no contrastada | H-14. **No tocado en esta revisión**: `data/` está fuera del alcance del revisor |

---

## Cómo se aplica cuando se apruebe

1. Nueva `spec.version_spec` (`0.1.1` si se aplica solo este diff; si se aplica junto al v1.2, va dentro de `0.2.0`).
2. Un ADR de aprobación con fecha y firma de Billy, que supera el estado `PROPUESTA` de `ADR-003`.
3. Tests: por cada INT nuevo, un test que compruebe que el motor lo **cita** donde la spec dice; para C1 y C2, un caso que cumple y uno que falla (`CLAUDE.md` §5).
4. `python evaluar_casos.py`: 7/7 y caso A en **305.829,6 kWh/año**. C1 y C2 son los únicos puntos que pueden moverlo; si lo mueven, ADR de ground truth antes de tocar `expedientes/_resultados_esperados/`.
5. `docs/04` §13 y `docs/HUECOS.md` actualizados en la misma sesión.
