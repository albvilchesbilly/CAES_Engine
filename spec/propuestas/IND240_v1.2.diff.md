# Propuesta de cambio: `spec/IND240_v1.1.yaml` → `IND240_v1.2.yaml`

**Estado: PROPUESTA, NO APROBADA.** Regla de oro 9: ningún cambio en la spec activa sin revisión de Billy. Este fichero describe el diff; no existe todavía `spec/IND240_v1.2.yaml`. Cuando se apruebe: se crea el fichero v1.2, se mantiene v1.1 para replay de actuaciones pasadas, se registra un ADR y se actualiza `docs/04` §11.

Origen: confrontación con la plataforma oficial (`docs/02`) y diseño de la cabecera transversal (`spec/propuestas/cabecera_v1.yaml`). Depende de que la cabecera se apruebe: sin cabecera no hay herencia.

| # | Elemento | v1.1 (actual) | v1.2 (propuesto) | Motivo | Riesgo |
|---|---|---|---|---|---|
| 1 | `spec.version_spec` | `0.1.0` | `0.2.0` | Cambio de reglas | — |
| 2 | `spec.version_ficha` | `"1.1"` | `"1.1"` (sin cambio: la ficha oficial no ha cambiado) | Solo cambia nuestra lectura | — |
| 3 | `spec.hereda` | — | `cabecera: cabecera_v1` | Variables y reglas comunes vienen de la cabecera | Requiere N1 con herencia |
| 4 | Variables `fecha_inicio_actuacion`, `fecha_fin_actuacion` | Declaradas en la ficha | Heredadas como `fecha_inicio_ejecucion`, `fecha_fin_ejecucion` (alias conservados para las reglas existentes) | Comunes a todas las fichas (art. 14.9.j) | Tests que citan los nombres antiguos |
| 5 | Variables `titular_nif`, `titular_razon_social` | Declaradas en la ficha | Heredadas como `propietario_inicial.nif/.razon_social` (alias conservados) | Cabecera | Idem |
| 6 | `R-TMP-01` | En la ficha | Absorbida por `R-CAB-02` | Duplicado | Ninguno (misma lógica) |
| 7 | `R-TMP-02` | En la ficha | Absorbida por `R-CAB-12` | Común a todas las fichas | Ninguno |
| 8 | `R-TMP-03` | `solicitud.fecha <= fecha_fin_actuacion + 3 años` · AVISO | Absorbida por `R-CAB-13`: `solicitud.fecha <= fecha(31, 12, anio(fecha_fin_ejecucion) + n_validez)` · AVISO · `interpretacion: INT-08` | La plataforma aplica "3 años desde el **año** de finalización, vencimiento el 31/12" (`docs/02` §5). Confirmar `n` con art. 17 Orden TED/815/2023 | Cambia el veredicto solo en actuaciones cerca del límite |
| 9 | `R-CON-05` | `unique(titular_nif)` · BLOQUEANTE_DATOS | Absorbida por `R-CAB-08` | Cabecera | Ninguno |
| 10 | `R-DOC-01` | Sin metadatos de diferencial | `diferencial: false`, `equivalente_plataforma: "TODO(API-04)"` | La plataforma comprueba presencia por tipo (`docs/02` §4) | Ninguno (no cambia la evaluación) |
| 11 | Todas las reglas | Sin `fase` ni `nivel` explícitos | `fase` y `nivel` según `docs/04` §5 (hoy se aplican por defecto) | Hacer explícito lo que el motor ya hace | Ninguno |
| 12 | `interpretaciones` | INT-01..07 | + `INT-08` (expiración) y `INT-09` (inversión) — declaradas en la cabecera, referenciadas aquí | Nuevas interpretaciones | — |
| 13 | Reglas restantes (`R-AMB`, `R-DOC-02..05`, `R-EVD`, `R-CON-01..04, 06, 07`, `R-CAL`) | — | **Sin cambios** | — | — |

**Resultado**: IND240 v1.2 tiene 22 reglas propias + 13 heredadas de la cabecera (de las cuales R-CAB-01, 02, 08, 12, 13 cubren lo que antes hacían R-TMP-01/02/03 y R-CON-05). El veredicto de los 7 casos del banco de pruebas **no debe cambiar** salvo que la cabecera introduzca carencias nuevas (p. ej. `precio_contrato_cesion` ausente en el convenio sintético → `R-CAB-03` SUBSANABLE); en ese caso, o se completa el paquete sintético o se documenta el cambio de ground truth en un ADR.

**Cómo se aplica cuando se apruebe** (`/sprint S3.2`):

1. Mover `spec/propuestas/cabecera_v1.yaml` → `spec/cabecera_v1.yaml`.
2. Crear `spec/IND240_v1.2.yaml` con los cambios de la tabla; conservar `spec/IND240_v1.1.yaml`.
3. N1 resuelve la versión aplicable (regla de vigencia: decisión abierta, `ADR-001` §3); hasta entonces, la CLI acepta `--spec` explícito.
4. Tests: los 7 casos con v1.1 y con v1.2; diferencias esperadas documentadas.
5. ADR nuevo con fecha de aprobación y firma de Billy.
